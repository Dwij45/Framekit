export default function AssetsPage() {
  return (
    <section>
      <h1>Assets</h1>
      <p className="lede">
        Empty on purpose. An asset is metadata in Postgres pointing at a file
        in object storage. Upload lands in Phase 1.
      </p>
    </section>
  );
}
