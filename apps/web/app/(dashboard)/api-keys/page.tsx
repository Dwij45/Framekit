export default function ApiKeysPage() {
  return (
    <section>
      <h1>API keys</h1>
      <p className="lede">
        Phase 4. Keys will look like <code>fk_live_…</code>, stored hashed,
        shown once — same idea as Stripe.
      </p>
    </section>
  );
}
